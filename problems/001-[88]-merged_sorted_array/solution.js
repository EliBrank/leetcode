/**
 * @param {number[]} nums1
 * @param {number} m
 * @param {number[]} nums2
 * @param {number} n
 * @return {void} Do not return anything, modify nums1 in-place instead.
 */
const merge = function(nums1, m, nums2, n) {
  
};

const testNums, testNums2 = [1,2,3,0,0,0];
console.log('CASE 1:')
console.log('majority element:', majorityElement(testNums));

console.log();
